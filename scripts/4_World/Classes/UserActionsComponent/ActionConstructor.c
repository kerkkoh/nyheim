modded class ActionConstructor {
    override void RegisterActions(TTypenameArray actions) { 
        super.RegisterActions(actions);
        // actions.Insert(ActionLaunchMissile);
        
        // Disabled due to bugs :(
        //actions.Insert(ActionSoundAlarm);
    }
}